# 每日 03:30；Persistent=true：错过的触发（关机/维护窗口）在下次启动后补跑一次
[Unit]
Description=blog daily backup timer (03:30, persistent)

[Timer]
OnCalendar=*-*-* 03:30:00
Persistent=true
AccuracySec=1min
Unit=blog-backup.service

[Install]
WantedBy=timers.target
